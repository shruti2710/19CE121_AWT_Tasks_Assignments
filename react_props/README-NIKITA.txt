install node modules by just typing npx create-react-app react-props on terminal.
then create components folder inside src folder and make another file named Tweets.js inside folder components.
now inside Tweets.js type rfc and u'll be having autogenerated code of it ..edit it as per the avilable file of Tweet.js
import React from 'react'

const Tweets=()=> {
    return (
        <div>
            
        </div>
    )
}
export default Tweets;

Like this..

Now we want to include this component in App function so open App.js file and write <Tweets /> inside it to include it
import logo from './logo.svg';
import './App.css';
import Tweets from './components/Tweets';

function App() {
  return (
    <div className="App">
        <h1>Hello From React</h1>
        <Tweets/>      
     
    </div>
  );
}

export default App;

like this ..
ok now run it ...terminal me likho command 
it is react so 
npm start



ok now  suppose we have to pass name from App.js to components/Tweets.js file so we are gonna make a variable inside 
App.js file i.e. 

inside function
const name = "Nikita" //e.g
now we are suppose to pass this in Tweets.js so

in the below line , U can see we have to make an attribute to Tweets file ..You can name it any 
suppose myName = {...} inside this braces , you are suppose to write the name of variable (in which u store the actual name)
<Tweets myName = {name}/>    

now moving ahead, we have to pass this myName attribute in Tweets.js file but we have to pass props as a parameter to make it understand that we r accessing
myName attribute.

import React from 'react'

const Tweets=(props)=> {
    return (
        <div>
            <h1>Name : {props.myName}</h1>

        </div>
    )
}
export default Tweets;


Like this..

and done u'll be able to see NAme : Nikita on webbrowser
now try with another file so creating a new file named Tweet.js inside components folder ok

use same syntax that of Tweets.js
(type rfc and change accordingly)

import React from 'react'

const Tweet=()=> {
    return (
        <div>
            
        </div>
    )
}
export default Tweet;

Like this..


so first of all make changes in Tweets.js

  <h1>Name in Tweets Component  : {props.myName}</h1> //write like this and 
  

  inside Tweet.js file

  import React from 'react'

const Tweet=(props)=> {
    return (
        <div>
            Name :{props.Tweetname}
        </div>
    )
}
export default Tweet;

now we want to transfer name to Tweet.js also so ... accessing it from Tweets.js (not App.js)
so what we have to do is just write following code in Tweet.js and Tweets.js
----------------
Tweets.js
----------------
import React from 'react'
import Tweet from './Tweet';

const Tweets=(props)=> {
    return (
        <div>
          <h1> Name in Tweets Component : {props.myName}</h1>
          <Tweet Tweetname = {props.myName}/>
        </div>
    )
}
export default Tweets;
--------------------------------
Tweet.js
--------------------------------

import React from 'react'

const Tweet=(props)=> {
    return (
        <div>
            Name in Child Tweet Component :{props.Tweetname}
        </div>
    )
}
export default Tweet;


-----------------------------------------

Now we want to pass an whole object named 
const tweets={} in Tweets.js


 const tweets = [
        {name:'Nikita',tweet:'I love backend and database design'},
        {name:'Shikha',tweet:'I love frontend and Web designing'},
        {name:'Sonia',tweet:'Blockchain is the new world for technology'},
        {name:'Ananya',tweet:'React Native is an advancement in app making'}
    ]

    this object we have to pass in props

